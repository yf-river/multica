// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: chat_idempotency.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const completeChatIdempotencyRecord = `-- name: CompleteChatIdempotencyRecord :one
UPDATE chat_idempotency_record
SET response_status = $7,
    response_body = $8::jsonb
WHERE workspace_id = $1
  AND actor_type = $2
  AND actor_id = $3
  AND operation = $4
  AND idempotency_key = $5
  AND request_hash = $6
  AND response_status IS NULL
  AND response_body IS NULL
RETURNING workspace_id, actor_type, actor_id, operation, idempotency_key, request_hash, response_status, response_body, created_at
`

type CompleteChatIdempotencyRecordParams struct {
	WorkspaceID    pgtype.UUID `json:"workspace_id"`
	ActorType      string      `json:"actor_type"`
	ActorID        pgtype.UUID `json:"actor_id"`
	Operation      string      `json:"operation"`
	IdempotencyKey pgtype.UUID `json:"idempotency_key"`
	RequestHash    string      `json:"request_hash"`
	ResponseStatus pgtype.Int4 `json:"response_status"`
	ResponseBody   []byte      `json:"response_body"`
}

func (q *Queries) CompleteChatIdempotencyRecord(ctx context.Context, arg CompleteChatIdempotencyRecordParams) (ChatIdempotencyRecord, error) {
	row := q.db.QueryRow(ctx, completeChatIdempotencyRecord,
		arg.WorkspaceID,
		arg.ActorType,
		arg.ActorID,
		arg.Operation,
		arg.IdempotencyKey,
		arg.RequestHash,
		arg.ResponseStatus,
		arg.ResponseBody,
	)
	var i ChatIdempotencyRecord
	err := row.Scan(
		&i.WorkspaceID,
		&i.ActorType,
		&i.ActorID,
		&i.Operation,
		&i.IdempotencyKey,
		&i.RequestHash,
		&i.ResponseStatus,
		&i.ResponseBody,
		&i.CreatedAt,
	)
	return i, err
}

const lockChatIdempotencyRecord = `-- name: LockChatIdempotencyRecord :one
SELECT workspace_id, actor_type, actor_id, operation, idempotency_key, request_hash, response_status, response_body, created_at FROM chat_idempotency_record
WHERE workspace_id = $1
  AND actor_type = $2
  AND actor_id = $3
  AND operation = $4
  AND idempotency_key = $5
FOR UPDATE
`

type LockChatIdempotencyRecordParams struct {
	WorkspaceID    pgtype.UUID `json:"workspace_id"`
	ActorType      string      `json:"actor_type"`
	ActorID        pgtype.UUID `json:"actor_id"`
	Operation      string      `json:"operation"`
	IdempotencyKey pgtype.UUID `json:"idempotency_key"`
}

func (q *Queries) LockChatIdempotencyRecord(ctx context.Context, arg LockChatIdempotencyRecordParams) (ChatIdempotencyRecord, error) {
	row := q.db.QueryRow(ctx, lockChatIdempotencyRecord,
		arg.WorkspaceID,
		arg.ActorType,
		arg.ActorID,
		arg.Operation,
		arg.IdempotencyKey,
	)
	var i ChatIdempotencyRecord
	err := row.Scan(
		&i.WorkspaceID,
		&i.ActorType,
		&i.ActorID,
		&i.Operation,
		&i.IdempotencyKey,
		&i.RequestHash,
		&i.ResponseStatus,
		&i.ResponseBody,
		&i.CreatedAt,
	)
	return i, err
}

const reserveChatIdempotencyRecord = `-- name: ReserveChatIdempotencyRecord :one
INSERT INTO chat_idempotency_record (
    workspace_id,
    actor_type,
    actor_id,
    operation,
    idempotency_key,
    request_hash
) VALUES ($1, $2, $3, $4, $5, $6)
ON CONFLICT DO NOTHING
RETURNING workspace_id, actor_type, actor_id, operation, idempotency_key, request_hash, response_status, response_body, created_at
`

type ReserveChatIdempotencyRecordParams struct {
	WorkspaceID    pgtype.UUID `json:"workspace_id"`
	ActorType      string      `json:"actor_type"`
	ActorID        pgtype.UUID `json:"actor_id"`
	Operation      string      `json:"operation"`
	IdempotencyKey pgtype.UUID `json:"idempotency_key"`
	RequestHash    string      `json:"request_hash"`
}

// The row is intentionally incomplete while its transaction is open. A
// concurrent request with the same key waits on the primary-key conflict and
// can only observe the completed row after the first transaction commits.
func (q *Queries) ReserveChatIdempotencyRecord(ctx context.Context, arg ReserveChatIdempotencyRecordParams) (ChatIdempotencyRecord, error) {
	row := q.db.QueryRow(ctx, reserveChatIdempotencyRecord,
		arg.WorkspaceID,
		arg.ActorType,
		arg.ActorID,
		arg.Operation,
		arg.IdempotencyKey,
		arg.RequestHash,
	)
	var i ChatIdempotencyRecord
	err := row.Scan(
		&i.WorkspaceID,
		&i.ActorType,
		&i.ActorID,
		&i.Operation,
		&i.IdempotencyKey,
		&i.RequestHash,
		&i.ResponseStatus,
		&i.ResponseBody,
		&i.CreatedAt,
	)
	return i, err
}
